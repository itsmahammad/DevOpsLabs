all:
  vars:
    ansible_user: azureuser
    ansible_ssh_private_key_file: ~/.ssh/id_rsa_casp
    ansible_python_interpreter: /usr/bin/python3

  children:
    frontend:
      hosts:
        vm-casp-frontend-dev:
          ansible_host: 10.10.2.10

    backend:
      hosts:
        vm-casp-backend-dev:
          ansible_host: 10.10.3.10

    ops:
      hosts:
        vm-casp-ops-dev:
          ansible_host: 10.10.5.10
